# Basic Redis configuration for ValidQR local development
# No persistence required for cache

bind 127.0.0.1
port 6379

# Disable saving to disk (cache only mode)
save ""
appendonly no

# Max memory limits
maxmemory 256mb
maxmemory-policy allkeys-lru
